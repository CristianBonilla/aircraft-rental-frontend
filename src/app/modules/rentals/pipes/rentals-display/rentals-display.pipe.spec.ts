import { RentalsDisplayPipe } from './rentals-display.pipe';

describe('RentalsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RentalsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
