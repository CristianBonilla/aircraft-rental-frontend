import { UsersWithRolePipe } from './users-with-role.pipe';

describe('UsersWithRolePipe', () => {
  it('create an instance', () => {
    // const pipe = new UsersWithRolePipe();
    // expect(pipe).toBeTruthy();
  });
});
